# type the below two lines into your terminal:
# conda env create --file jag_env.yml
# conda activate jag

name: jag_cap
dependencies:
  #- tensorflow
  - spacy
  - nltk
  #- gensim
  - python
  #- numpy
  - pandas
  - scikit-learn
  - jupyter
  - matplotlib
  - seaborn
  - scipy
  - ipython
  - scikit-learn-intelex
  - pip
  - plotly
  - pip:
      - wordcloud
      - tensorflow==1.14.0
      - gensim==3.6.0
      - numpy==1.19.5
# then run the test.ipynb to verify correct installaltion, not forgetting to change the notebook kernel to tf

# If you update the `conda_env.yml` you can run the following to update the
# environment with your changes:
# conda env update --file conda_env.yml --prune
