# type the below two lines into your terminal:
# conda env create --file yml_files/pytorch_env.yml
# conda activate pytorch

name: pytorch
dependencies:
  - pytorch
  - python
  - numpy
  - pandas
  - jupyter
  - ipython
  - pip
  - pip:
    - transformers==3.0.0

# then run the test.ipynb to verify correct installaltion, not forgetting to change the notebook kernel to tf

# If you update the `pytorch_env.yml` you can run the following to update the
# environment with your changes:
# conda env update --file yml_files/pytorch_env.yml --prune